#include <bits/stdc++.h>
#define let const auto
#define REP(var, i) for (int var = 0; var < i; ++var)
using namespace std;
using Int = int;
//using Int = long long;
using vi = vector<Int>;
using vvi = vector< vector<Int> >;

/**
 * Solution for problem: aaah
 *
 * Written by: ZZZ
 *
 * Date: 2018-12-14 06:34:40.946010 UTC
 *
 * Lessons learned: TEXTTEXTTEXT
 */
void solve() {
    string a, b;
    cin >> a >> b;

    if (a.length() >= b.length()) {
        cout << "go" << endl;
    } else {
        cout << "no" << endl;
    }
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    solve();
}
